# Wizard v1 Selection Model

`WizardSelectionV1` is the frontend/backend source-of-truth:

- `templateId`
- `selectedOptionIds`
- `optionVersions`
- `contextVars`

Resolution output is persisted in `WizardLockfile` and consumed by preview/export.
