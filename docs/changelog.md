# Changelog

## Unreleased

- Introduced v1 architecture split (`contracts`, `engine`, `plugins`, `server`, `web`).
- Added manifest-driven plugin packs and deterministic resolution/generation engine.
- Added strict lockfile and compatibility policy checks.
